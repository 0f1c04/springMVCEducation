<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosta.account">
    <update id="update1">
        update CUST_ACCOUNT
        set BALANCE = BALANCE - 1
        where ACCOUNTNO = '112'
    </update>
    <update id="update2">
        update CUST_ACCOUNT
        set BALANCE = BALANCE + 1
        where ACCOUNTNO = '113';
    </update>
</mapper>